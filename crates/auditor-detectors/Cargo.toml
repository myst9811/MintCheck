[package]
name = "auditor-detectors"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
auditor-parser = { path = "../auditor-parser" }
serde = { version = "1", features = ["derive"] }
