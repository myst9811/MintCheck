//! JSON / Markdown serialization of audit findings.
//!
//! Depends only on `auditor-detectors` (see `PROJECT_SPEC.md` §2.2).

#[cfg(test)]
mod tests {
    #[test]
    fn test_scaffold() {
        assert_eq!(2 + 2, 4);
    }
}
